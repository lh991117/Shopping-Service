spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        '[format_sql]': true

logging:
  level:
    '[org.hibernate.SQL]': debug
    # (옵션) 바인딩 파라미터까지 보고 싶으면:
    # org.hibernate.orm.jdbc.bind: trace
